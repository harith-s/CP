#include <iostream>
#include <vector>
#include <map>
#include <queue>
#include <set>
using namespace std;

int minimumLengthEncoding(vector<string> &words)
{
    vector<int> index(words.size(), 0);

    // basically I have to check for suffixes here. 
}

int main()
{
    return 0;
}
